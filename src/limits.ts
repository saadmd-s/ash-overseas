/** Shared user-facing input limits; sized for the Workers Free D1 query budget. */
export const MAX_TRANSACTION_LINES = 25;
export const MAX_NAME_LENGTH = 200;
export const MAX_TEXT_LENGTH = 500;
export const MAX_NOTES_LENGTH = 4000;
